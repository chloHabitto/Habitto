import Foundation
import FirebaseAuth

// This would need to be run in the app context, but here's the code:
// let userId = Auth.auth().currentUser?.uid
// print("Your anonymous user ID: \(userId ?? "Not authenticated")")
